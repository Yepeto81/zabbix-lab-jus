all:
  hosts:
    zabbix_server_deb10:
      ansible_host: 192.168.30.22
      ansible_user: yepeto
      ansible_become: yes
      zabbix_db_external: false
      zabbix_db_host: localhost
      zabbix_db_name: zabbix
      zabbix_db_user: zabbix
      zabbix_db_pass: "zabbix-pass"
      zabbix_timezone: "America/Argentina/Buenos_Aires"
      zabbix_server_name: "Zabbix 6.0 Server (Deb10)"

    zabbix_server_deb13:
      ansible_host: 192.168.1.184
      ansible_user: yepeto
      ansible_become: yes
      zabbix_db_external: false
      zabbix_db_host: localhost
      zabbix_db_name: zabbix
      zabbix_db_user: zabbix
      zabbix_db_pass: "zabbix-pass"
      zabbix_timezone: "America/Argentina/Buenos_Aires"
      zabbix_server_name: "Zabbix 7.0 Server (Deb13)"

  children:
    zabbix_servers:
      hosts:
        zabbix_server_deb10:
        zabbix_server_deb13:
